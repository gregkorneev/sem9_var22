# 🧮 Семинар 9 — Полный перебор

## Задача: Ханойские башни

Этот проект реализует решение задачи **Ханойских башен** методом **полного перебора**, а также содержит модуль для **экспериментального анализа** роста сложности алгоритма.

Проект включает:

* 🚩 реализацию классического рекурсивного алгоритма, выполняющего `2^N - 1` ходов
* 📊 автоматическую генерацию таблицы результатов в формате **CSV**
* 📝 автоматическое создание текстового отчёта в **Markdown**
* 📁 чистую структуру проекта (src, include, data)
* 🔧 сборку через **CMake**

---

# 📂 Структура проекта

```
sem9/
 ├── src/
 │    ├── main.cpp              # Точка входа
 │    ├── hanoi.cpp             # Реализация алгоритма Ханойских башен
 │    ├── experiments.cpp       # Генерация csv + отчёта
 │
 ├── include/
 │    └── hanoi.h               # Заголовочный файл
 │
 ├── data/
 │    ├── csv/                  # Таблица результатов экспериментов
 │    │    └── hanoi_results.csv
 │    └── hanoi_report.md       # Markdown-отчёт
 │
 ├── CMakeLists.txt
 ├── .gitignore
 └── README.md
```

---

# 🚀 Как собрать и запустить проект

Чтобы полностью пересобрать проект и получить свежие результаты, выполните:

```bash
rm -rf build && rm -rf data && cmake -B build && cmake --build build && ./build/nqueens && python3 .py/plot_nqueens.py && python3 .py/plot_nqueens_dashboard.py && python3 .py/plot_nqueens_table.py && python3 .py/plot_nqueens_growth.py && python3 .py/plot_nqueens_metrics.py
```

Эта команда:

1. Удаляет предыдущую сборку
2. Удаляет старые результаты экспериментов
3. Генерирует новую сборку
4. Запускает программу

---

# 🧠 Возможности программы

### ▶ 1. Решение задачи Ханойских башен для заданного N

* Для `N ≤ 5` выводятся **все ходы**
* Для `N > 5` выводится только:

  * число ходов,
  * теоретическая оценка,
  * время работы

### ▶ 2. Экспериментальный анализ сложности

При запуске программа предложит выполнить серию экспериментов:

* N = 1 … maxN
* измерение времени
* сравнение с теорией `2^N - 1`
* формирование файлов:

```
data/csv/hanoi_results.csv
data/hanoi_report.md
```

---

# 📊 Что содержится в CSV

Файл:

```
data/csv/hanoi_results.csv
```

содержит таблицу вида:

| N | Ходов (программа) | Теория (2^N-1) | Время, мс |
| - | ----------------- | -------------- | --------- |

Отсюда удобно строить графики и анализировать экспоненциальный рост сложности.

---

# 📝 Что содержится в Markdown-отчёте

Файл:

```
data/hanoi_report.md
```

включает:

* общую формулу сложности
* выводы по экспериментам
* обсуждение практического предела
* сравнение алгоритмов и обоснование оптимальности

---

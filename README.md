# Семинар 9 — Полный перебор. Задача N ферзей

Этот проект реализует классическую задачу **расстановки N ферзей на шахматной доске N×N** так, чтобы они не били друг друга.
Решение выполняется методом **полного перебора с отсечениями (backtracking)**, затем проводится **экспериментальный анализ сложности**, формируется **CSV-таблица**, **отчёт** и **визуализации**.

Проект выполнен как полноценная мини-исследовательская работа, включая автоматическую генерацию всех графиков.

---

# 📁 Структура проекта

```
sem9_var22/
│
├── src/
│   ├── main.cpp                # диалог с пользователем
│   ├── nqueens.cpp             # алгоритм backtracking + один эксперимент
│   ├── experiments.cpp         # серия экспериментов, CSV, отчёт
│
├── include/
│   ├── nqueens.h               # объявления публичных функций
│
├── .py/
│   ├── plot_nqueens.py              # 3 базовых графика: время, проверки, комбинированный
│   ├── plot_nqueens_dashboard.py    # логарифмический дашборд
│   ├── plot_nqueens_table.py        # таблица CSV → PNG
│   ├── plot_nqueens_growth.py       # график коэффициентов роста
│   ├── plot_nqueens_metrics.py      # доп.метрики (время на проверку, log решений и др.)
│
├── data/
│   ├── csv/
│   │   ├── nqueens_results.csv      # создаётся программой
│   ├── png/                         # все визуализации
│   ├── nqueens_report.md            # текстовый отчёт
│
├── CMakeLists.txt
├── README.md
└── .gitignore
```

---

# 🧠 Основная идея задачи

Задача N ферзей требует расставить N ферзей так, чтобы:

- ни один не бил другой по **строке**,
- ни по **столбцу**,
- ни по **диагоналям**.

Для её решения используется **рекурсивный перебор с отсечениями**:

- ставим ферзя построчно,
- для каждой позиции проверяем безопасность,
- при конфликте не углубляемся дальше.

Это классическая NP-трудная задача с экспоненциальной сложностью, отлично подходящая для изучения методов перебора.

---

# 🛠 Зависимости

### C++:
- компилятор (GCC / Clang) с поддержкой C++17
- CMake ≥ 3.10

### Python:
```bash
pip install matplotlib
```

---

# 🚀 Как полностью пересобрать и запустить проект

### **Большая команда** (обновлённая версия)

Выполняет полный цикл:

- пересборка C++-части,
- запуск N ферзей,
- генерация CSV,
- генерация отчёта,
- построение всех графиков.

```bash
rm -rf build && rm -rf data && cmake -B build && cmake --build build && ./build/nqueens && python3 .py/plot_nqueens.py && python3 .py/plot_nqueens_dashboard.py && python3 .py/plot_nqueens_table.py && python3 .py/plot_nqueens_growth.py && python3 .py/plot_nqueens_metrics.py
```

---

# 🔍 Запуск частичных шагов

### Запуск только программы:

```bash
./build/nqueens
```

### Генерация графиков, если CSV уже есть:

```bash
python3 .py/plot_nqueens.py
python3 .py/plot_nqueens_dashboard.py
python3 .py/plot_nqueens_table.py
python3 .py/plot_nqueens_growth.py
python3 .py/plot_nqueens_metrics.py
```

---

# 📦 Результаты работы

После запуска ты получишь:

### **CSV таблица**
```
data/csv/nqueens_results.csv
```

### **Отчёт**
```
data/nqueens_report.md
```

### **PNG-визуализации**
В папке `data/png/`:

- nqueens_time.png
- nqueens_states.png
- nqueens_combined.png
- nqueens_dashboard.png
- nqueens_table.png
- nqueens_growth.png
- nqueens_metrics.png
